{
  "name": "portfolio",
  "version": "1.0.0",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./dist/output.css --minify && cp -R src/* dist/"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.0"
  }
}